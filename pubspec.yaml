name: easywrt
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  go_router: ^16.3.0
  http: ^1.5.0
  logger: ^2.6.2
  path_provider: ^2.1.5
  synchronized: ^3.4.0
  path: ^1.9.1
  hive: ^2.2.3
  uuid: ^4.5.1
  mobx: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.6
  source_gen: ^1.5.0

flutter:
  uses-material-design: true